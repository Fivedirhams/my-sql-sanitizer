# CloakDB dependencies
# Core logic is stdlib-only (json, re, hashlib, datetime, subprocess, pathlib, time, random).
# rich is required for the interactive CLI wizard (cloaker/__main__.py).
rich>=13.0.0
